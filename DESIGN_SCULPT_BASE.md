# FaceLint — 捏脸基值通道重排（Sculpt Base Relay）设计

**状态**: v0.6 代码已落地 + v0.6.2 门限设计（共线门）+ v0.6.3 逐顶点 α；RINDO 实测反馈已并入；待 Unity 编译验证
**日期**: 2026-05-20（v0.6.2 修订 2026-08-15）
**前置讨论**: 用户目标与多轮推演收敛（见会话记录，核心结论已并入本文）

---

## 0. 目标（验收标准）

> 在 mesh blendshape 上**一次捏脸**（设置基值），构建后**无需修改/修复任何**：
> 眨眼（内置/MMD）、MMD 世界驱动、单帧表情、多帧动态表情（商品）。

**一句话不变量（v0.6.2 修订，分两支）**：

```
同通道驱动（眨眼族）： 你的脸(w) = 你的捏脸 + (1 − b)·作者脸(w)   ← 仿射映射，w=100 恰好是作者峰值
异通道表情（>_< 等）：  你的脸(w) = 你的捏脸 + 作者脸(w)           ← 纯加性，与编辑器所见完全一致
```

- w = 0（静止）→ 恰好是你的捏脸（两支一致）
- **为什么分两支**：RINDO 实测证明，对方向与捏脸不一致的表情（如 `>_<`，delta 里闭眼分量之外还有眉/颊特征）
  做均匀缩放会把整个表情压弱（"显著不足"）；用户认可的是编辑器里的加性结果。
  通道判定见 §1 的双指标门。

---

## 1. 核心方程

所有 blendshape 共享同一份顶点预算，任何时刻的位移为：

```
V(w) = Σ_i w_i · D_i          （D_i = shape i 的 delta，含法线/切线帧）
```

- **捏脸预算**：`B = Σ_{j∈sculpt} b_j · D_j`（b_j = 用户捏脸基值 / 100）
- **驱动通道缩放**（对每个被动画驱动的 shape i）：

```
α_i = 1 − Σ_j b_j · (D_j·D_i) / |D_i|²      （投影公式）
```

- 三个 delta 完全相同时退化为 `α = 1 − Σb_j`（例：eye_close=20 → α=0.8）
- 所有 b=0 时 α=1 —— **退化为现有 `MmdBlendShapeScalePass` 的行为，向后兼容**
- `Σb_j > 1`（过度捏脸）→ α clamp 到 0

### 1.1 双指标门（v0.6.2，α 的适用边界）

投影公式对"驱动通道与捏脸同通道"是精确的，但对**方向部分重叠**的驱动（模型自己的表情）
会把整个 delta 均匀缩小——闭眼分量修对了，表情特征（眉/颊/眼形）被一起压弱。
RINDO 实测：`>_<`（基值 0，商品动画驱动）构建后"显著不足"。

**门限**：驱动通道 i 与**任一**捏脸 shape j 满足 `max(A, B, C) ≥ 0.95` 才应用 α，否则 α = 1：

```
A = (D_j·D_i) / (|D_j|·|D_i|)    余弦 —— 对幅度不敏感（同向不同幅的孪生 shape）
B = (D_j·D_i) / |D_i|²           驱动侧覆盖 —— 单眼驱动 ⊂ 双眼捏脸（B=1）
C = (D_j·D_i) / |D_j|²           雕塑侧覆盖 —— 双眼驱动 ⊃ 单眼捏脸（C=1；用户模型有 eye_close + eye_close_L/R 三件套）
```

| 驱动通道 | A | B | C | max | 判定 |
|---|---|---|---|---|---|
| 眨眼孪生（まばたき vs eye_close，同向） | 1.0 | 1/k | k | 1.0 | α ✓ |
| 单眼（眼L 驱动 ⊂ 双眼捏脸） | 0.707 | **1.0** | 0.5 | 1.0 | α ✓（α=1−b 正好修单眼过闭） |
| 双眼（eye_close 驱动 ⊃ 眼L 捏脸） | 0.707 | 0.5 | **1.0** | 1.0 | α ✓（逐顶点只缩左半边，右眼 1.0） |
| 表情（>_<，带正交特征） | ~0.8 | ~0.8 | ~0.8 | <0.95 | **α=1，原样保留** ✓ |

**关键性质**：α 只依赖"哪些 shape 被捏了、捏了多少"——一次捏脸后眨眼族自动正确；
模型/商品自己的表情零改动（纯加性，编辑器所见即构建所得）。

### 1.2 逐顶点 α（v0.6.3，非对称捏脸）

门通过的通道内部，缩放是**逐顶点**的：

```
α(v) = clamp( 1 − Σ_j b_j · (D_j(v)·D_i(v)) / |D_i(v)|² , 0, 2 )
      |D_i(v)|² ≈ 0 的顶点 → α = 1（无驱动位移，无需缩放）
```

**为什么标量不够**（RINDO 实测）：`eye_close_L` 捏 10，驱动通道是（近似）双眼的 shape。
标量 α=0.9 应用于整个 delta → 左眼 1.0 正确，右眼 0.9 留缝——"缝的大小正好是左眼多出来的量"。
逐顶点形式：有预算的顶点（左眼）→ 0.9，预算不触及的顶点（右眼）→ **1.0**。

- 对称捏脸 → 逐顶点退化回均匀 `1−Σb`，与旧版逐字节一致
- 表情的额外特征（E 部分，`D_j(v)=0`）→ α=1 自动保留——门之外再上一层保险

### 1.3 预算全量（v0.6.5 定稿：烘焙与补偿必须一致）

**教训链（RINDO 实测）**：
- v0.6.2 标量公式：21 项全局点积把 |D_i|² 撑爆 → 19 个表情清零（标量伪影）
- v0.6.4 预算门：只收同通道项 → 与 Pass A 全量烘焙不一致 → eye_open（撑眼，反向）被烤入却不补偿 → 右眼差一点
- **定稿**：预算**全量**（逐顶点）。反向项点积为负 → α(v)>1 自动补回；跨通道项只影响自己触及的顶点；
  顶点被用户基值真正饱和（预算>驱动位移）时 α=0 属正确语义（无法更闭）

**规则**：`α(v) = clamp(1 − Σ_所有 j b_j·(D_j(v)·D_i(v))/|D_i(v)|², 0, 2)`，烘焙同式。
门（§1.1）降级为诊断信息（是否同通道）；逐顶点局部性即天然的门。

---

## 2. 两个 Pass（全部在 NDMF Transforming 阶段，MA 合并之后）

### Pass A — 顶点烤（Sculpt Freeze，唯一架构）

> **硬约束：WD OFF 不考虑**——WD ON 是绝大多数商品与 MA 系工具的默认，要求 WD OFF 等于切割生态。
> 因此私有频道方案（依赖"没有任何 clip 写它"）作废；雕塑必须活在顶点空间，动画层永远碰不到。

对每个捏脸 shape j（基值 b_j > 0，无论是否驱动通道）：

1. 克隆 mesh 后，`vertices += b_j · deltaPos_j`，`normals += b_j · deltaNorm_j`（逐顶点归一化），`tangents += b_j · deltaTan_j`（相对新法线正交化）
2. **禁止 `Mesh.RecalculateNormals()`**——会毁掉卡通模型的 edited normals；delta 法线/切线累加是 AAO FreezeBlendShape 已验证的路线
3. shape **保留**（商品仍驱动它），渲染器权重归零
4. 零基值 shape 不烤、不动

**WD 免疫原理**：雕塑在顶点里；WD ON 状态进场把未动画属性写回烘焙默认值（通常 0），0 权重 = 已烤的中性脸，怎么写都碰不到雕塑。

### Pass B — 驱动缩放（现有 pass 的扩展）

对全部驱动通道按 §1 的 α_i 缩放 delta（**经 §1.1 双指标门判定后**）。现有 `MmdBlendShapeScalePass.cs` 的流式克隆/重建机制原样复用，改动点：

1. `scales` 字典从"64 个 MMD 名"放开到"任意 shape 名"（MMD 名仅作 UI 预分组）
2. 读 renderer 基值 → 计算 B → 逐通道投影 → 门限判定 → α_i（未过门者 α=1）
3. 执行顺序：Pass A（烤雕塑）→ Pass B（缩驱动）→ Pass C（中和 controller）

### Pass C — Controller 中和（WD ON 的 default face 帧）

WD ON 状态下，各状态会写回**烘焙在 controller 里的默认值**。avatar 自己的 Base/Gesture 层往往烘焙了原版基脸（用户的"default face 帧"痛点）；顶点烤之后这些值仍会写回，叠在雕塑上（b + c·α）。

1. 构建时克隆 avatar 自己的 Base/Gesture 控制器
2. 对每个状态：雕塑用到的 shape 的烘焙默认值改写为 0（雕塑已替代原版基脸）
3. **商品 FX 层不碰**——保留商品自己的 look，让其经过仿射映射自然缩放
4. 技术：读取/改写 WD 烘焙默认值 = AAO Write Defaults pass 同款序列化技巧，已被验证可行

### 驱动通道检测

Transforming 阶段遍历 avatar 上全部 AnimatorController（此时 MA 已合并商品 FX）：

- 收集所有 clip 的 `blendShape.X` binding → 驱动名单
- 并入：`MmdShapeDatabase` 64 标准名 + `vrc.blink` / `vrc.blink_l` / `vrc.blink_r` 家族

---

## 3. 驱动方的验收映射（v0.6.2 修订）

| 驱动方 | 触发 | 现状问题 | 构建后 |
|---|---|---|---|
| 眨眼（内置/商品，同通道） | vrc.blink / 眼L / 眼R → 100 | 0.2 + 1.0 = 1.2 过闭 | 顶点 0.2 + 0.8 = 1.0 ✓（单眼同样 α=1−b） |
| MMD | まばたき → 100 | 同上 | → 1.0 ✓ |
| 单帧表情（同通道，如商品自驱动 eye_close） | gesture clip 单帧 key | 覆盖弹脸 / 叠加过闭 | 仿射 → 1.0 ✓ |
| 多帧动态表情（同通道） | 商品包逐帧 clip 0→100 | 每帧偏、峰值最严重 | 整条曲线 ×α，峰值收敛，**零逐帧修改** ✓ |
| **模型/商品自己的表情（异通道，如 `>_<`）** | 商品 clip → 100 | 无（作者设计即正确） | **α=1 原样保留**，与编辑器所见一致 ✓ |

**同 shape 场景**（捏脸与商品驱动同一个 shape，如三胞胎 eye_close/blink/まばたき）：Pass A 把 20 烤进顶点 + Pass B 把三个驱动 delta 都 ×0.8。静止 = 你的 20；任一驱动到 100 = 作者闭眼。

**异通道判定基准（RINDO 实测）**：`>_<` 基值 0、被商品动画驱动、构建后变弱——
根因是投影公式把整个 delta 均匀缩小；编辑器里"捏脸 + 表情全量"才是用户认可的结果。
因此异通道表情一律不缩放（§1.1 门）。

---

## 4. 诚实边界（验收条件，非缺陷）

1. **WD ON 为硬约束**（用户决策：WD OFF 切割 90% 商品生态，不考虑）。雕塑必须烤进顶点——WD ON 写 0 碰不到顶点，只有 Pass C 覆盖不到的"controller 烘焙基脸"需要中和。
2. **中间帧语义**：多帧表情的中间帧 = "从你的脸单调走到作者峰值"，非作者原曲线的中间帧。这是捏脸者想要的语义，但需在文档中明示。
3. **骨骼驱动表情**不在覆盖范围（本方案是 blendshape 领域）。
4. **商品自身的 look**（其状态烘焙的非零默认值）不中和，走仿射映射自然缩放——保留商品作者的设计，这是 Pass C 只碰 avatar 自己控制器的原因。
5. **异通道表情纯加性（v0.6.2）**：模型/商品自己的表情（方向与捏脸不一致）不缩放——闭眼过闭只发生在同通道；异通道表情在编辑器里看起来怎样，构建后就是怎样。这是 RINDO `>_<` 实测后的明确决策，不是妥协。
6. **门限 0.95 是启发式**：目前只在 RINDO 上验证过。阈值过低会漏修近孪生眨眼通道，过高会再次削弱强重叠表情；遇到边界案例（日志 cos/proj 落在 0.90~0.97）需要按实际模型调整。

---

## 5. 对 repo 的改动清单

| 文件 | 改动 |
|---|---|
| `Editor/MmdBlendShapeScalePass.cs` | + 驱动检测（扫 clip bindings）；+ Pass A 顶点烤（pos+normal+tangent 累加）；+ 投影 α 计算 |
| `Editor/`（新） | Pass C：controller 克隆 + WD 烘焙默认值改写（参考 AAO Write Defaults pass） |
| `Editor/MmdCalibratorWindow.cs` | 扫描放开到全部 blendshape，MMD 名仅作预分组，新增"其他/商品"分类 |
| `Runtime/MmdBlendShapeScaler.cs` | scales 语义从"MMD 名"扩为"shape 名"；雕塑输入 = 渲染器基值（构建时读取） |
| `DESIGN.md` / `DEVLOG.md` | 代码落地后补记 |

---

## 6. 未决问题

- [x] 投影公式对"方向部分重叠的驱动"过度缩放（RINDO `>_<` 实测）→ **v0.6.2 双指标门解决**（§1.1）
- [x] 单眼 shape（眼L/眼R）在门限下的误判（余弦 0.707）→ **B 指标（驱动能量占比）解决**；旧版纯投影下单眼本就正常（α=1−b），双指标门逐字节复刻该行为
- [x] 非对称捏脸（`eye_close_L`=10）右眼留缝 → **v0.6.3 逐顶点 α 解决**（§1.2）；根因是标量 α 无法表达分侧缩放
- [ ] 门限 0.95 在更多模型上的验证（RINDO 的驱动 shape 恰好坐在悬崖边 cos/proj ≈ 0.95——逐顶点已消除悬崖影响，但仍需更多样本）
- [ ] 逐顶点 α 与均匀 α 在对称场景的等价性回归（理论：D_j ⊥ D_i 顶点上 dot=0 → α=1，退化成立）
- [ ] Pass C 的序列化改写细节：WD 烘焙默认值在 AnimatorController 中的位置与 API（参照 AAO Write Defaults pass 实现）
- [ ] Pass C 的"avatar 自己 vs 商品"控制器判定（默认按 Descriptor 的 Base/Gesture 归属 + MA 合并来源）
- [ ] 投影公式在 60k 顶点 × 数百 shape 下的性能（已预计算 `|D_j|²`，O(shapes²) 次点积，一次性）
- [ ] 烤入顶点后与 viseme/blink 系统的交互验证（预期无冲突：雕塑是常量位移，驱动通道照常工作）
