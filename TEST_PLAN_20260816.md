# FaceLint — 明天测试计划（2026-08-16）

> 本文是 v0.6.1 → v0.6.5 全部改动的交接清单。测试目标只有一个：
> **一次捏脸，无需修复任何眨眼/MMD/表情**，且 RINDO 的右眼差一点问题消失。

---

## 0. 动手前（3 分钟）

1. **重新复制** `Editor/MmdBlendShapeScalePass.cs` 到测试工程（今天的日志是旧代码跑出来的，不复制 = 白测）
2. 其余文件没变（校准器/控制器中和/组件均无需动）
3. 场景里保持你现在的 21 个基值（Σ=5.52）——这是最真实的测试环境

---

## 1. 必测项（按顺序）

### ① 右眼差一点（本轮核心）
- **期望**：眨眼时左/右眼**完全闭合**，无缝隙
- 机制：v0.6.5 预算恢复全量——`eye_open`（撑眼基值）的负点积使 α(v)>1 自动补回
- **不过关**：把新日志贴回来，我加"左右眼分侧平均 α"日志再定位

### ② 表情不被清零（v0.6.4 的成果，确认没被 v0.6.5 破坏）
- 检查日志：`clamped to 0` 只应出现在**顶点真饱和**处（预算>驱动位移）
- 抽查：`eye_happy`、`eye_angry`、`じと目`、`ｷﾘｯ` 驱动到 100 应**有反应**
- 对照：v0.6.2 日志里这 19 个全被清零；v0.6.5 应全部恢复

### ③ `>_<` 回归
- 期望：仍正常（上轮实测通过；本轮它 sculptCov=2.405 过门走逐顶点，mean≈0.99，几乎无改动）

### ④ 非对称捏脸（可选，纯验证）
- `eye_close_L` 捏 10 → 眨眼 → 左/右眼都完全闭合（逐顶点 α 的原始动机场景）

### ⑤ 其他
- 静止 = 你的捏脸（21 个基值的脸）
- Pass C 日志：`Pass C (controller neutralization): 3 clip(s) rewritten` 应保持

---

## 2. 日志判读标准

```
[MMDBlinkFixer] α for 'eye_close': mean=0.775 (per-vertex, cos=1.000, driverCov=1.000, sculptCov=8.358).
[MMDBlinkFixer] α for 'eye_open':  mean=0.880 (per-vertex, ...)   ← 反向基值，α 应局部 >1
```

- `mean` 是全脸均值（大部分顶点 α=1），**不代表眼部的实际值**
- `[no same-channel sculpt; per-vertex only]` 后缀 = 该驱动无同通道捏脸，仅局部微调（正常）
- `Sculpt frozen: 21` 应保持；`Scaled` 数无硬性预期（100-200 都合理）

---

## 3. 当前代码状态（未提交）

| 文件 | 改动 |
|---|---|
| `Editor/MmdBlendShapeScalePass.cs` | v0.6.1-0.6.5：基值快照相关无；驱动检测补全（OverrideController+自定义层）；**门 max(A,B,C)≥0.95 → 逐顶点 α(v) → 预算全量**；CS0104 修复 |
| `Editor/MmdCalibratorWindow.cs` | 基值快照/恢复（防捏脸被清）；hasThumbnail 显式标记 |
| `Editor/MmdControllerNeutralizer.cs` | descriptor 自定义动画层中和（局限 #4） |
| `Runtime/MmdBlendShapeScaler.cs` | 注释修正 |
| `DEVLOG.md` | v0.6.1 → v0.6.5 全部记录 |
| `DESIGN_SCULPT_BASE.md` | 门/逐顶点/预算全量设计定稿 |
| `TECH_NOTES.md` | 新建：工程笔记（FBX 格式、API 核对、踩坑） |

**测试通过后建议提交**：
```
git add -A && git commit -m "v0.6.5: per-vertex alpha with full budget (bake/compensate consistency); calibrator weight snapshot; Pass C custom layers"
```

---

## 4. 设计演进速查（为什么是现在这样）

```
v0.6.2 标量 α + 门      → 表情被投影公式削弱（>_< 显著不足）→ 门
v0.6.3 逐顶点 α          → 标量无法表达"左×0.9 右×1.0"（右眼留缝）→ 逐顶点
v0.6.4 预算门            → 21 项全量预算清零 19 表情 → 只收同通道
v0.6.5 预算全量          → 门与烘焙不一致（eye_open 残留）→ 全量+逐顶点
```

**核心不变量（两分支）**：
- 同通道驱动（眨眼族）：`你的脸(w) = 捏脸 + (1−b)·作者脸(w)` → 100 = 作者峰值
- 异通道表情：原样保留（纯加性）
- 逐顶点 α(v) 统一两者：`1 − Σ_所有 b_j·(D_j(v)·D_i(v))/|D_i(v)|²`，反向基值自动 α>1

---

## 5. 如果还有问题（排查路线）

| 现象 | 可能原因 | 下一步 |
|---|---|---|
| 右眼还差 | 驱动多 shape 同时 100（叠算）或帧数据异常 | 加左右分侧日志 |
| 表情仍弱 | 顶点真饱和（你的基值过重） | 属正确语义，调基值 |
| `>_<` 变弱 | 逐顶点 α 触及眼部 | 恢复门保护（门+全量并存） |
| 编译错误 | 复制不完整/旧文件残留 | 重新复制全部 Editor/ |
